using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DistinceTravel : MonoBehaviour
{
    private float currentTimer;

    // Update is called once per frame
    void Update()
    {
        currentTimer = Time.fixedTime;

    }
}
